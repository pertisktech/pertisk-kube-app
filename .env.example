# Backend auth (required for production: set APP_ENV=production)
USERNAME=admin
PASSWORD=change-me
JWT_SECRET=replace-with-a-long-random-secret
APP_ENV=development

# Optional: listen ports
# PORT=15222
# GRPC_PORT=50051

# Optional: kubeconfig override
# KUBECONFIG=/path/to/kubeconfig
